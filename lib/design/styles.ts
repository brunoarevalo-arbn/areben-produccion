// Clases de estilo compartidas para evitar repetir strings de Tailwind.

/** Encabezado chico de sección / header de tabla (uppercase, tracking). */
export const HEADING_XS = 'text-xs font-bold uppercase tracking-widest text-stone-400';

/** Card base: fondo blanco, borde stone, radio 2xl. */
export const CARD = 'bg-white rounded-2xl border border-stone-200';

/** Fila de listado con densidad estándar. */
export const ROW = 'px-4 md:px-5 py-3';
